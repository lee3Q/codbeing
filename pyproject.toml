[project]
name = "codbeing"
version = "0.1.0"
description = "Local-first DecisionTrace capture and Runtime Analysis CLI."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7"]

[dependency-groups]
dev = ["pytest>=7"]

[tool.pytest.ini_options]
testpaths = ["tests"]
